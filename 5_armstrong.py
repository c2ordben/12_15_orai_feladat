import unittest

def armstrong(n:int)->bool:
    digits = str(num)
    n = len(digits)
    total = sum(int(digit) ** n for digit in digits)
    return total == 





class TestFibo(unittest.TestCase):




unittest.main()
